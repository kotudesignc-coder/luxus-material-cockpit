import { PlaceholderPage } from "@/components/placeholder-page";

export default function FlowPage() {
  return (
    <PlaceholderPage
      href="/flow"
      plannedDay="Day 6"
      blurb="三顆箭頭：Input（拍照上傳）→ Design（選色 / 比較 / AI 推薦）→ Output（存圖 / 分享 LINE）。點哪顆可以跳到對應的子頁。"
    />
  );
}
